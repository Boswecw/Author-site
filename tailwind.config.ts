// tailwind.config.ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{svelte,ts,js,html}'],
  theme: {
    extend: {}
  },
  plugins: []
};

export default config;
