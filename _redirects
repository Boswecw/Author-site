# optional legacy cleanups
/home        /            301
/books.html  /books       301
/blog.html   /blog        301

# (only keep this if you actually have Netlify functions under netlify/functions)
# /api/*      /.netlify/functions/:splat   200

# SvelteKit catch-all (must be last)
 /*         /.netlify/functions/server   200
